{
  "dependencies": {
    "del": "^6.0.0",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-typescript": "^6.0.0-alpha.1",
    "merge-stream": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "gulp build",
    "clean": "gulp build:clean"
  }
}
